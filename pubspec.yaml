name: test_koran
description: "A new Nasio project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.7.0
  firebase_auth: ^5.3.2
  google_sign_in: ^6.2.2
  firebase_analytics: ^11.3.4
  cloud_firestore: ^5.4.5

  shared_preferences: ^2.0.0
  audioplayers: ^6.1.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/microwave_button.mp3
